import { HeaderTitleDirective } from './header-title.directive';

describe('HeaderTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderTitleDirective();
    expect(directive).toBeTruthy();
  });
});
